def kinetic_energy(m : float, v : float) -> float:
    """
    [FUNC] kinetic_energy:
    Returns the kinetic energy in Joules
    
    Where:

    Mass     = m
    Velocity = v
    """

    return (m * (v*v)) / 2